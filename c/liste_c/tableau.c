#include <stdio.h>
#include <stdlib.h>

struct tableau {
    size_t taille;
    char **tab;
};

void create_plateau(){
    struct *tableau = malloc(sizeof(struct tableau));
    tableau->taille = 0;
    tableau->tab = NULL"
}
